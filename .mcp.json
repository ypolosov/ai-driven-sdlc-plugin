{
  "mcpServers": {
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {}
    },
    "sdlc-state-rag": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@ypolosov/sdlc-state-rag"
      ],
      "env": {
        "SDLC_STATE_RAG_DSN": "${SDLC_STATE_RAG_DSN}"
      }
    }
  }
}
